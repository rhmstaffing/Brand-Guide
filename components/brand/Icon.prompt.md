Outline icon from the Lucide set (CDN substitute; RHM has no icon library of its own).
```jsx
<Icon name="hard-hat" size={24} color="var(--rhm-blue)" />
```
- Uses currentColor by default, so it follows text color.
- Stick to 20 to 28px in UI, 32 to 40px in feature grids.

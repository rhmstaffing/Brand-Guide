Modal panel over an ink scrim for confirmations and short forms.
```jsx
<Dialog open={open} title="Request sent" onClose={close} actions={<Button onClick={close}>Got it</Button>}>A recruiter will call you within one business day.</Dialog>
```
